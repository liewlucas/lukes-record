/// <reference path="../.astro/types.d.ts" />

/** Injected by `vite.define` in astro.config.mjs: true when STORYBLOK_TOKEN was set at build time. */
declare const __STORYBLOK_ENABLED__: boolean;
/** Injected by vite.define: which content version build-time fetches use. */
declare const __STORYBLOK_VERSION__: 'draft' | 'published';
